apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiny-requestbin.fullname" . }}
  labels:
    {{- include "tiny-requestbin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tiny-requestbin.selectorLabels" . | nindent 4 }}
