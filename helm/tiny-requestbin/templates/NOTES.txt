Tiny RequestBin has been deployed!

{{- if .Values.httpRoute.enabled }}

HTTPRoute is enabled. Make sure your Gateway is properly configured.
{{- range .Values.httpRoute.hostnames }}
  Visit: http://{{ . }}
{{- end }}
{{- end }}

To access via port-forward:

  kubectl port-forward svc/{{ include "tiny-requestbin.fullname" . }} 8282:{{ .Values.service.port }}

Then open http://localhost:8282 in your browser.

Send requests to any path (except /) to capture them:

  curl http://localhost:8282/test -d '{"hello": "world"}'
